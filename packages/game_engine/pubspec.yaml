name: game_engine
description: >
  Pure Dart rules engine for Armor Up!, a biblical spiritual-warfare themed
  take-that card game. No Flutter or network dependencies.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
