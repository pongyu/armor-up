org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler crashes (IllegalArgumentException: "this and
# base files have different roots") when the project and the pub-cache live
# on different drive letters, which nsd_android's Kotlin sources trigger.
# Disabling incremental compilation avoids the crash at the cost of slower
# rebuilds.
kotlin.incremental=false
